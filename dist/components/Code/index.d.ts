export = CreateCode;
declare function CreateCode({ copyButtons, theme }: {
    copyButtons: any;
    theme: any;
}): (props: any) => JSX.Element;
declare namespace CreateCode {
    function sanitize(sanitizeSchema: any): any;
}
